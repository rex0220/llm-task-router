# 記事作成の進捗状況がわからない

> ✅ 対応済み（v0.2.22〜0.2.24）: 正本 `progress.events.jsonl` から `progress.json`/`progress.md` を再生成し、`llm-task-router article:status --run <id>` で現在地・各工程の開始/終了/所要・概算コストを表示。進捗ファイルは工程開始時に生成（done を待たない／v0.2.23）。

時間がかかっていても、どの程度記事作成が進んでいるのか不明
どの処理に時間に時間がかかっているのか知りたい（開始・終了・処理時間）
ログファイルでその記事の進捗状況を把握したい

現在地：7工程中 6番目の途中
工程	状態	根拠
1. 企画・profile確定	✅ 完了	profile: qiita
2. create → refine	✅ 完了	refine R1 で score 11 / issue 0 / approved（clean）
3. final-review 確認	✅ 完了	final-review.md（07:19）
4. factcheck＋build-verify → revise	✅ 完了	revise-instruction-1（07:42）
5. 編集レビュー → 採用分 revise	✅ 完了	final.md 更新（07:45）
6. 再factcheck → 台帳正規化	🔄 進行中	claims.raw.json（07:48)・sources.raw.json（07:49）は再生成済み。ただし正規化済みの claims.json / sources.json はまだ無い
7. publication-check → verify-artifacts → GO/NO-GO → export	❌ 未着手	publication-check.md なし／verify-artifacts 未実行


# ファクトチェックに時間がかかる

> ✅ 対応済み（v0.2.22）: 初回 factcheck 後に `article:factcheck-stamp` で baseline を受理し、以降は `article:factcheck-scope` が `full|skip|diff` を判定（差分ゼロなら skip＝二度手間回避）。

同じファクトチェックを行っていて、無駄
1回目と前提が変わらないのなら、2回目は不要では無いか？

# 時間がかかりすぎる

> ✅ 一部対応（v0.2.22）: 高コストな factcheck/build の前に `article:direction-check`（テーマ適合・構成・読者を判定する軽量ゲート）を通せる。各工程の実測「所要」は progress に記録（factcheck の事前見積は未対応）。

ファクトチェック前に、ドラフト記事をみて、記事の方向性をチェックしたい
ファクトチェックの処理時間見積

# API の費用を知りたい

> ✅ 対応済み（v0.2.23〜）: progress.md の「概算$」列＋「概算コスト合計」行、`runs/router.log` に呼び出し毎の `cost_usd`。処理毎と合計の両方を表示（usage トークン×料金のローカル概算）。

ログに表示できないか？
処理毎に表示して合計も表示

# ClaudeCode の使用量をログに残せないか？

> ✅ 対応済み（v0.2.24 / #16）: progress.md に「トークン(in/out)」列＋「トークン合計」行を追加（工程毎に積算・LLM工程のみ）。`runs/router.log` には呼び出し毎の input/output tokens。schema repair 時は初回＋修復の使用量を合算して計上。

どの程度記事作成に使用量を消費しているかログに残せるか？

# コマンドの実行承認が多すぎる

記事作成用フォルダー内の操作は自動承認にしたい

# 完成報告もログに追加

> ✅ 対応済み（v0.2.24 / #16）: `article:completion-report` 生成時に `completion-report` の done イベントを progress に1行記録（GO/NO-GO を note に）。報告本体は `runs/<id>/completion-report.md`（runs/ に閉じ、export/index.json には混ぜない）。

ログに残したい

## 完成報告の例

記事が全ゲートを通過して完成しました。最終版を確認しました。

完成報告：2026-06-20-climate-action-myths
記事: 「温暖化対策のウソ」を検証する — 効果が過大評価されがちな対策と、データで効く対策の見分け方
ファイル: final.md

ゲート結果（全パス）
ゲート	結果
refine	clean 停止（round 1/3、major+ 0件）/ final-review approved
factcheck（Web裏取り）	初回 critical 2+major 1 を検出 → revise で IPCC AR6(2019)単一セットに統一・IEA(2024)で定量化 → 再検証 BLOCKING 0、主要数値 verified
build-verify（実機）	Python 2ブロックを隔離venvで実行 → passed（「高炭素電源で拮抗〜逆転」を再現）
editorial-review（別provider claude-opus-4-8）	publication-candidate。採用4件（出典粒度・地政学橋渡し・コード対称化・刈り込み）
claims-normalize	10 claims / 12 sources / blocking 0
verify-artifacts	OK
構成（要望反映済み）
導入 → 排出の全体像（部門別） → 評価軸4点 → 過大評価されがちな対策（プラ削減・地産地消・EV誤解・小さな節電）→ 効く対策（発電脱炭素・断熱/ヒートポンプ・移動転換・食・産業）→ 経済地政学：誰が利益を得るのか（太陽光パネルの中国シェア約80%超/ウェハー約97%、コバルト採掘DRC約70%・精錬は中国、レアアース精錬約90%超 ＝ IEA 2024）→ 注意点 → チェックリスト

要望の「誰がどの国が利益を得ているのか」は**「経済地政学として見る温暖化対策」**節として、産出国と精錬国のズレを出典つきの桁で可視化。陰謀論調を避け「受益者がいる＝対策がウソではない／コスト・依存・供給リスクを値踏みする材料」と明示しています。

編集長から1点、判断を上げたい論点があります
編集レビューで挙がった W001（コードを記事の主役＝実データ分析に格上げ） は、qiita（技術媒体）のエンジニア読者の期待には合うものの、企画方針（コードは説明用の仮想モデルに留める）を変えることになるため、編集長判断で保留＝ユーザー上申としています。

概算コスト約 $0.85。GO 推奨です。article:export（公開相当）はユーザー承認後に実行します。

